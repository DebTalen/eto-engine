#ifndef ETO_RENDERER_HPP
#define ETO_RENDERER_HPP

/* 
#include <components/CLight.hpp>
#include <components/CRenderable.hpp>

#include <vector>
#include <memory>
#include <algorithm>

using std::shared_ptr;

namespace eto
{

class Renderer 
{
public:
	static Renderer &get_instance();


	void add_cLight(shared_ptr<CLight> pLight);
	void remove_cLight(shared_ptr<CLight> pLight);

	void add_cRenderable(shared_ptr<CRenderable> pRend);
	void remove_cRenderable(shared_ptr<CRenderable> pRend);

	void render();
private:
	Renderer();

	std::vector<shared_ptr<CLight>>	    m_lights;
	std::vector<shared_ptr<CRenderable>> m_rends;
};
}
 */

#endif 
